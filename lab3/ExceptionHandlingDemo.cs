using System;

namespace Bhumika.Unit_3
{
    internal class ExceptionHandlingDemo
    {
        /*static void Main(string[] args)
        {
            int a = 0, b = 0; // Initialize variables

            try
            {
                Console.WriteLine("Enter two integers:");
                a = int.Parse(Console.ReadLine()); 
                b = int.Parse(Console.ReadLine()); 

                int c = a / b; // Perform division
                Console.WriteLine($"Result: {c}");
            }
            catch (DivideByZeroException dex)
            {
                Console.WriteLine("Divide By Zero Error: " + dex.Message);
            }
            catch (FormatException e)
            {
                Console.WriteLine("Format Error: " + e.Message);
            }
            catch (Exception e)
            {
                Console.WriteLine("Error: " + e.Message);
            }
            finally
            {
                Console.WriteLine("This is the final exception message.");
            }

            Console.ReadKey();
        }*/
    }
}
